# Pip dependencies for ds_count_v4_brand.py.
#
# Install JetPack, CUDA, TensorRT, DeepStream, GStreamer Python bindings,
# OpenCV, and the matching pyds wheel separately on the Jetson device.
# Do not put full `pip freeze` output here.

numpy==1.19.5; python_version < "3.8"
numpy>=1.23,<2; python_version >= "3.8"

pycuda==2022.1
